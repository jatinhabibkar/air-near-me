import React, { Fragment } from 'react';
const  Spinner= () => (
  <Fragment>
      <i className="material-icons" style={{verticalAlign:"middle"}}><img
      src="./socialicon/spinner.svg"
      style={{ width: '30px', margin: 'auto', display: 'block' }}
      alt='Loading...'
    /></i>
    
  </Fragment>
);

export default Spinner